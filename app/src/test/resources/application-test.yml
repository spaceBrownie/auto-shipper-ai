spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://127.0.0.1:5432/autoshipper_test}
    username: ${TEST_DB_USERNAME:autoshipper}
    password: ${TEST_DB_PASSWORD:autoshipper}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true

logging:
  level:
    com.autoshipper: DEBUG
